この輪講フォルダでは、私が2026/08/17に担当する輪講の準備をしています。あなたは輪講の資料作成サポートをしてもらいます。
作るものは全て260817フォルダの中に作成します。
出力は全て日本語（スライド本文・コードコメント・手順書すべて）。

## テーマ

「webアプリのセキュリティ入門」 — SQLインジェクション / XSS / CSRF の3つを扱う。

貫くメッセージは2つだけ：

- **入力を「データ」として扱わず「コード」として解釈させてしまう** → SQLi・XSS
- **そのリクエスト、本当にウチの画面から来た？を確認していない** → CSRF

## ゴール（参加者が持ち帰るもの）

1. 3つの攻撃を**自分の手で成功させる**。「本当に動いてしまう」を体験する
2. **自分の手で直して、攻撃が失敗するのを確認する**
3. 明日から自分のPRで見る**3つのチェックポイント**を持ち帰る
   - SQL文を文字列連結していないか → プレースホルダを使う
   - ユーザー入力をHTMLとして描画していないか → エスケープする / `innerHTML` を使わない
   - 状態を変えるリクエストの出どころを確認しているか → SameSite Cookie + CSRFトークン

各テーマは必ず **攻撃する → 自分で直す → もう一度攻撃して失敗を見る** の3ステップで構成する。
「怖い話を聞いた」で終わらせない。

## 進め方（確定事項）

- **参加者は各自のローカルで動かす。** デプロイはしない
- **事前準備の案内を出す。** 当日までに clone → `npm install` → `npm start` で画面が出るところまで確認してもらう。当日のセットアップは0分の前提
- **修正は参加者自身がやる。** 発表者が直して見せるのではなく、手を動かしてもらう
- **保険として発表者が Cloudflare Tunnel を1本立てておく。** 当日セットアップに失敗した人はそのURLを叩いて、攻撃パートだけでも参加できるようにする
  ```bash
  cloudflared tunnel --url http://localhost:3000
  ```
- **ブラウザは Chrome 指定。** Safari は Cookie 周りの挙動が違って事故る

## 時間配分（合計30分・質疑は別枠5分）

| 時間 | 内容 |
|---|---|
| 3分 | 導入：なぜ他人事じゃないか。デモアプリの紹介 |
| 6分 | SQLインジェクション（説明2 → 攻撃1.5 → 自分で修正2 → 再攻撃0.5） |
| 7分 | XSS（説明2 → 攻撃2 → 自分で修正2.5 → 再攻撃0.5） |
| 5分 | CSRF（説明2 → 罠ページを踏む1 → 自分で修正1.5 → 再確認0.5） |
| 4分 | まとめ・持ち帰りチェックリスト |

計25分。時間は必ず溢れるので、この余裕を残したまま作ること。

## 成果物の構成

```
260817/
├── README.md    参加者に事前共有する案内（セットアップ手順）
├── slides/      Marp形式のMarkdown（VS Codeでプレビュー、PDF出力可）
├── app/         脆弱なチャットアプリ（localhost:3000）
├── attacker/    CSRF用の罠ページ（127.0.0.1:4000）
└── handson/     当日の手順書（コピペ用ペイロード + 修正箇所の案内 + 解答）
```

`localhost:3000` と `127.0.0.1:4000` はブラウザ的に**別サイト扱い**になるので、ローカルでもCSRFが成立する。
（`localhost:3000` と `localhost:4000` だと同一サイト扱いになって成立しない。ポートはsiteの判定に入らないため）

## デモアプリ

自作のチャットアプリ。**Node + Express + SQLite（better-sqlite3）+ 素のHTML/CSS/JS。**

- ORMやテンプレートエンジンの自動エスケープは**使わない**。フレームワークが守ってくれると、なぜ危ないかが見えなくなるため
- 依存は最小限にする。`npm install` が重いと事前準備で脱落者が出る
- ダミーデータのみ。実在の個人情報・社内データは一切入れない

仕込む脆弱性：

| 攻撃 | 仕込む場所 |
|---|---|
| SQLi | ログイン（`' OR '1'='1` が通る） |
| XSS | メッセージ表示（stored XSS、`innerHTML` で描画） |
| CSRF | 「全メッセージ削除」エンドポイント（トークンなし） |

### 参加者が自分で直せるようにする工夫

- 脆弱な箇所に `// 【課題3】 SQLインジェクション` のようなマーカーコメントを置く。手順書から grep で飛べるように（絵文字は使わない方針）
- 手順書には**ファイル名と行番号**を明記する。探す時間で潰れないように
- `handson/解答.md` に修正後のコードを載せる。**詰まった人がコピペで追いつけること**を優先する。置いていかれる人を作らない
- 修正後はサーバー再起動が要らないようにする（`--watch` で自動リロード）

### LLMは呼ばない

チャットアプリの見た目にはするが、AIの応答は**固定文字列のスタブ**にする。理由：

- 意図的にSQLiできるアプリに本物のAPIキーを置くのは危険（DBやenvから抜かれる）
- 課金・レイテンシ・API障害を輪講に持ち込まない
- 30分のテーマはWeb脆弱性であってLLMではない

LangChainも使わない（複雑さが増えるだけで、脆弱性の説明には何も足さない）。
プロンプトインジェクションは**おまけ枠でスライド1枚だけ**。時間が余ったら触れる程度。

### 絶対に仕込まない脆弱性

OSコマンドインジェクション、任意ファイル読み書き、`eval` 系。
参加者のPC（および Tunnel 使用時は発表者のPC）が実際に乗っ取られる。
今回扱う SQLi / XSS / CSRF はいずれもPC本体には手が届かない範囲に収まっている。

## 参加者向けの注意

スライドに1枚入れる：「ここで学ぶ攻撃は、自分が権限を持つ環境でのみ試すこと。他人のサイトで試すと犯罪です」

## 前提（未確認・仮置き）

- 参加者10人程度、フロント/バック混在
- 対面

違っていたら私が指摘するので、この前提で進めてよい。

## 実装状況（2026/08/15 時点：一通り完成・動作確認済み）

- `app/` … 動作確認済み。`cd app && npm install && npm start` で 3000/4000 が起動
  - better-sqlite3 はプリビルドが入り、コンパイル不要だった（install 約8秒）
  - 脆弱箇所は `【課題1】`/`【課題2】`/`【課題3】` で検索して飛べる
- 攻撃ペイロード（検証済み）:
  - SQLi: ログインのユーザー名に `admin' --` → 管理者でログイン成功
  - XSS: チャットに `<img src=x onerror="alert('XSSされました')">` → 実行（stored）
  - CSRF: `http://127.0.0.1:4000` を開くと全メッセージ削除
- CSRFの成立原理: 罠ページが `location.href` でトップレベルGETナビゲーション →
  SameSite=Lax でもクロスサイトのトップレベルGETにはCookieが乗るため成立。
  修正は GET→POST（server.js と app.js の2か所）。POST化するとナビゲーションで呼べず、
  さらに別サイトからのPOSTは SameSite=Lax がCookieを落とすので防げる。
- 未検証: ブラウザ上でのXSS実行とCSRFナビゲーションは curl では再現できないので、
  当日前に一度 Chrome で通しリハーサルをすること。
- 変更したら壊れやすい所: `localhost:3000`（アプリ）と `127.0.0.1:4000`（罠）の
  ホスト名の違いがCSRF成立の肝。ポートだけ変えて両方 localhost にすると成立しなくなる。

## スライドの流儀（2WINS社内ルールに準拠）

`/Users/kinhyonu/2wins/輪講/資料作成_勉強会.pdf`（「良いスライドの作り方」）に沿って作成済み。
`slides/slides.md`（Marp）がこのルールを実装している。編集後は `slides/export.sh` でPDF再生成。
納品形式は **PPTX と PDF の両方**（`slides/slides.pptx` / `slides/slides.pdf`）。
PPTX は各スライドが画像として入る（Googleスライド等で文字の再編集は不可・発表と共有は可）。
導入は「実害 → Webの仕組み（正常フロー図）→ 脆弱性とは（3攻撃を流れ上にマップ）→ 進め方」で本題に入る。

守っているルール：
- 1スライド1メッセージ。上部に パンくず（小・青）→ メッセージ（1文・キーワードを青/オレンジで着色）→ ボディ（図表中心）。左に青バー
- 色は3色：ベース=無彩色グレー / テーマ=青(#1f5c8b, #2e7db5) / アクセント=オレンジ(#e2792b)。赤は使わない
- 節ごとに目次を再掲し現在地だけ黒太字、他はグレーアウト
- 問いスライド、NG/OK 2列比較（NG=グレー見出し/OK=青見出し）、全体像マップ＋現在地ハイライト
- 図は `slides/diagrams/*.svg`（4枚）。箱矢印ではなくフラットイラスト（人物・ブラウザ・サーバー・DB・封筒・クッキー・虫など）で、大きく配置。3色パレット・絵文字なし
- 絵文字は全成果物（スライド・アプリUI・手順書）で不使用。[[slide-illustration-preference]] に準拠
